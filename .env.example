# ============================================================================
# PBRNeRF Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your actual values
# Usage: source .env or load it in your SLURM scripts
# ============================================================================

# ----------------------------------------------------------------------------
# Cache Directories
# ----------------------------------------------------------------------------
# These directories store temporary files and cached data to speed up training
# Make sure these paths exist and have sufficient storage space

# Matplotlib configuration and cache
MPLCONFIGDIR=/workspace/cache/matplotlib

# PyTorch model cache and extensions
TORCH_HOME=/workspace/cache/torch
TORCH_EXTENSIONS_DIR=/workspace/cache/torch_extensions
PYTORCH_KERNEL_CACHE_PATH=/workspace/cache/pytorch_kernels

# Hugging Face models and datasets cache
HF_HOME=/workspace/cache/huggingface

# Tiny CUDA Neural Networks RTC cache
TCNN_RTC_CACHE_DIR=/workspace/cache/tcnn_rtc_cache

# ----------------------------------------------------------------------------
# Weights & Biases (W&B) Configuration
# ----------------------------------------------------------------------------
# W&B is used for experiment tracking and visualization
# Sign up at https://wandb.ai/ to get your API key

# W&B cache directory
WANDB_CACHE_DIR=/workspace/cache/wandb

# Your W&B username or team name
WANDB_ENTITY=your-username-or-team

# Project name for organizing experiments
WANDB_PROJECT=

# Your W&B API key (keep this secret!)
# Get it from https://wandb.ai/authorize
WANDB_API_KEY=your_wandb_api_key_here

# Optional: Add notes to your runs
WANDB_NOTES="Experiment description"

# Optional: Enable/disable W&B (online, offline, disabled)
# WANDB_MODE=online

# Optional: Set run name
# WANDB_RUN_NAME=my-experiment-name

# Optional: Add tags to your runs
# WANDB_TAGS=tag1,tag2,tag3

# ============================================================================
# Additional Configuration (Optional)
# ============================================================================

# Uncomment and modify as needed:

# CUDA settings
# CUDA_VISIBLE_DEVICES=0

# OpenCV settings
# OPENCV_IO_ENABLE_OPENEXR=True

# Number of threads for data loading
# NUM_WORKERS=8

# Mixed precision training
# USE_AMP=True

# ============================================================================